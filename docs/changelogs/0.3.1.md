# 0.3.1
- **Bug Fix**: Fixed `E0107` missing generics error for `InlineSlice` trait bounds on `Mutex` views, resolving compilation failures on the Stable Rust toolchain.
- **Benchmarks**: Added comprehensive `criterion` suite for benchmark over standard vector structs.
