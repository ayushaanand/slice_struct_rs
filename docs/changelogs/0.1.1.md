# 0.1.1

## Fixed
* Fixed the GitHub repository URL in `Cargo.toml` and `slice_struct_macro/Cargo.toml` to point to the correct upstream repository (`https://github.com/ayushaanand/slice_struct_rs.git`).
