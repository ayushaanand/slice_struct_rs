# 0.1.0

## Added
* Initial release of the `slice_struct` macro and runtime library.
* Support for packing multiple dynamically sized slices into a single heap allocation.
* Zero-cost, contiguous memory layout for maximum cache locality.
